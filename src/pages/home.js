import React from 'react';
import Header from '../components/header/header'

const Home = ({ onSwitchMode }) => {
    return (
        <div className='Home'>
            <Header />
        </div>
    );
};

export default Home;
